# .env — for local SQLite only (no Azure)
USE_LOCAL_ARK=True
ARK_API_KEY=

# These are IGNORED when using SQLite, but keep them empty or remove
AZURE_SQL_SERVER=
AZURE_SQL_DATABASE=
AZURE_SQL_USERNAME=
AZURE_SQL_PASSWORD=

# Optional: force SQLite path (recommended when using OneDrive)
SQLITE_DB_PATH=greencare_local.db